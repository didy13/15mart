git push{

    dodavanje novih stvari na github

    git add ("."dodaje sve sto ste radili)/("style.css"dodaje samo fajl koji ste menjali)
    git commit -m "Komenatar sta si promeni dodao/promenio"
    git push origin main
} 

git pull{

uzimanje promenjenih fajlova sa gita


git pull

}